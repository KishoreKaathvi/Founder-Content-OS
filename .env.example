# Optional. When set, KSE simulation / reports / grounding and Content OS
# campaign briefs use Gemini. When unset, high-fidelity local fallback runs
# for both Signal Radar and Content Studio (no crash).
GEMINI_API_KEY=

# Optional public URL if you deploy this app (not required for local dev).
APP_URL=http://localhost:3000
